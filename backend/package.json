{
  "name": "special1-api",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker API backed by Neon Postgres for special1",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "node scripts/migrate.mjs",
    "db:seed": "node scripts/seed.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260921.1",
    "typescript": "^5.6.3",
    "wrangler": "^4.136.2"
  }
}